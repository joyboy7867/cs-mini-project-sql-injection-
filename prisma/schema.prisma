generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Products {
  product_name String
  brand        String?
  description  String
  uniq_id      String  @id @db.VarChar
  retail_price Int?
}
